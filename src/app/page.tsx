import Typography from "@/components/Typography";

export default function Home() {
  return (
    <div>
      <Typography variant="h1" font="body"> testing body</Typography>
      <Typography variant="h1" font="title" > testing title</Typography>
    </div>
    
  );
}
